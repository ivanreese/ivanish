# ivy.boo

A deeply personal website.

Install deps with `npm`. Build with `cake`. Deployed to Vercel on commit. DNS on Cloudflare. Domain on Hover.

`cdn.ivy.boo` is CNAME'd to `d3um8l2sa8g9bu.cloudfront.net`
